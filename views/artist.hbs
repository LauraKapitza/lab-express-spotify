<h1>Your Artist Search Results</h1>
  <ul>
      {{#each artists}}
        <div>
            <p>{{name}}</p>
            <img src="{{images.[0].url}}" alt"{{name}}" />
            <a href="/albums/{{id}}">View Albums</a>
        </div>     
      {{/each}}
  </ul>