<h1>Albums for: {{albums.[0].artists.[0].name}}</h1>
  <ul>
    {{#each albums}}
        <div>
            <p>{{name}}</p>
            <img src="{{images.[0].url}}" alt"{{name}}" />
            <a href="">View Tracks</a>
        </div>     
    {{/each}}
  </ul>